/* This is the firstname of the user */
const user = 'John';

/* This is the lastname of the user */
const surname = 'Smith';

console.log(user + ' ' + surname);

/*when you run the code, it will log both the user and surname variables to the console at the same time. 
The + operator is used to concatenate the values of user and surname,
and the space ' ' is added to separate the first name and last name when they are printed to the console. */